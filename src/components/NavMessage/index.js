// Require in dependencies
import React from "react";
// import "./style.css";

function NavMessage(props) {
  return (
    <p>{props.message}</p>
  );
}

// Export ClickItem
export default NavMessage;